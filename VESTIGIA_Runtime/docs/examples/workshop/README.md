# Canonical recursive hello

These design examples bind one resident ritual to one approved script. The script only reads typed
input and returns structured JSON. The ritual pauses before the optional workspace write.

They are acceptance fixtures, not executable installation instructions. Import and activation remain
separate lifecycle actions.
