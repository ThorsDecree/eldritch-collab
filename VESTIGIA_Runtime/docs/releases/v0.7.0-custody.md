# VESTIGIA Runtime v0.7.0 Release Custody Runbook

Status: **live gate complete; publication custody pending**

This document turns issue #5 into an executable release-custody sequence. It does not redefine the v0.7.0 code release, rebuild artifacts, or substitute later v0.8 development artifacts for the stable v0.7.0 release.

## 1. Canonical release identity

- Release: `v0.7.0` — The Resident's Drawers
- Release PR: #13
- Release branch head: `c0d5138bd66bf5eadc9bdc5bc7ffdb094ce6a8d2`
- Validated merge commit on `main`: `748e5d74392ad4f0a98c75b187f82b91606e9e39`
- Green PR merge ref: `10497a2f7208d98364ebc13eba8a16b0c527cf63`
- Historical source fixture: v0.6.1 commit `b12f8f778c390f4ac8fc3abe1af1a44a73799c26`
- Release-candidate Runtime CI run: `30857072690` (run 34)

The PR discussion records a direct comparison between the green PR merge ref and the actual `main` merge commit with **no file differences**. They are distinct merge commits over the same release tree.

The eventual `v0.7.0` tag MUST identify the validated v0.7.0 release commit selected by maintainers. It MUST NOT identify current v0.8 development canon merely because `main` has moved forward.

## 2. Live Windows trust-boundary gate

The live gate is complete.

PR #13 records two evidence tranches. The completed tranche states that the Windows deployment was operator-verified by checksum against release head `c0d5138bd66bf5eadc9bdc5bc7ffdb094ce6a8d2` and exercised every release-ledger row with redacted evidence.

### Ambient source visibility

- `source.visibility=hidden` — Pass.
- `source.visibility=all_channel` — Pass.
- `source.visibility=mentions_only` — Pass on isolated history; an earlier contaminated trial is explicitly excluded.
- `source.visibility=allowlisted_only` — Pass.
- oversized ambient envelope preservation — Pass.

### Reaction authorization and delivery

- react to current trigger — Pass.
- add then remove reaction — Pass.
- reject non-visible target before outward action — Pass.
- react to visible ambient message — Pass.
- failed Discord platform delivery produces an independent failure receipt while ordinary resident text survives — Pass.

### Private-image challenge binding

- challenge creation without disclosure — Pass.
- expiry rejection — Pass.
- wrong-channel destination rejection — Pass.
- later resident confirmation through another participant's doorway — Pass.
- one-time consumption and replay rejection — Pass.

No deterministic result was relabeled as live evidence. The repository validation record should carry the redacted receipt/event references from PR #13 rather than leaving those rows marked `Pending`.

**Live Discord trust-boundary gate: PASS.**

## 3. Preserved release-candidate artifact custody

The original release-candidate artifact remains available in GitHub Actions and was independently re-verified during issue #5 cleanup.

Artifact:

```text
vestigia-runtime-0.7.0-10497a2f7208d98364ebc13eba8a16b0c527cf63
```

GitHub artifact ZIP SHA-256:

```text
ac9ab04d5fe1e49f31423e389599c0d1649ad27bd2c58dab17adf82af3282e09
```

Contained files and independently rechecked SHA-256 values:

```text
8cfe00302ce886c419760910994a61851dd3d0083404e38d724cc905fed5769b  vestigia_runtime-0.7.0-py3-none-any.whl
941cba3356b90948f35a8b86986f0561e7ed74d05ced04410faab23213004f13  vestigia_runtime-0.7.0.tar.gz
```

The included `SHA256SUMS.txt` records those same two hashes.

The upgrade-canary artifact also remains available:

```text
v061-to-v070-upgrade-canary-10497a2f7208d98364ebc13eba8a16b0c527cf63
sha256:de93007f5ba5566779c3917d49cceb6ffc56a2b163d4bd7ed529a4547ec3b5d4
```

This confirms the release-candidate artifact identity. It does **not** silently replace the stricter post-merge custody rule recorded in PR #13.

## 4. Post-merge artifact checkpoint

PR #13 records that the workflow on merge commit `748e5d74392ad4f0a98c75b187f82b91606e9e39` was configured to run on pushes to `main` and should have produced:

```text
vestigia-runtime-0.7.0-748e5d74392ad4f0a98c75b187f82b91606e9e39
v061-to-v070-upgrade-canary-748e5d74392ad4f0a98c75b187f82b91606e9e39
```

The remaining custody task is to locate that exact post-merge distribution artifact, confirm the corresponding `main` run succeeded, download it, and independently verify its ZIP and contained payload hashes.

If that exact post-merge artifact is no longer available, maintainers MUST make an explicit release-custody decision. Do not silently substitute a later v0.8 build. Any fallback must document why it is equivalent to the validated v0.7 release tree and what evidence replaces the missing post-merge artifact.

## 5. Final publication sequence

1. Fold the completed redacted live ledger from PR #13 into `v0.7.0-validation.md`.
2. Locate and verify the exact post-merge `main` artifact from commit `748e5d74392ad4f0a98c75b187f82b91606e9e39`.
3. Record the `main` workflow run ID, artifact name, artifact ZIP digest, and contained wheel/sdist hashes in this document.
4. Obtain maintainer review of the custody evidence.
5. Merge this custody documentation PR to current `main`.
6. Create tag `v0.7.0` at the validated v0.7.0 release commit selected in section 1, not at current v0.8 `main`.
7. Create GitHub Release `VESTIGIA Runtime v0.7.0 — The Resident's Drawers` from that tag.
8. Attach exactly:
   - `vestigia_runtime-0.7.0-py3-none-any.whl`
   - `vestigia_runtime-0.7.0.tar.gz`
   - `SHA256SUMS.txt`
9. Download the public release attachments and verify both payload hashes again.
10. Update `README.md` and `ROADMAP.md` so `v0.7.0` is described as both historical development canon and publicly published distribution.
11. Close issue #5 as completed.

## 6. Release-note minimum

The public release notes should include:

- the release name and compatibility floor;
- Windows and Python 3.11/3.14 validation statement;
- the genuine v0.6.1 upgrade/pack/restore validation statement;
- the completed live Discord trust-boundary statement;
- a link to `v0.7.0-validation.md`;
- the SHA-256 values for the published wheel and source distribution;
- the security boundary that v0.7.0 does not grant unrestricted host execution;
- a note that later v0.8 development work is not part of this tagged release.

## 7. Explicit non-actions

Do not:

- tag current v0.8 `main` as `v0.7.0`;
- rebuild a later v0.8 wheel and relabel it as v0.7.0;
- leave the repository validation ledger marked pending when live evidence already exists;
- include secrets or private Discord content in the evidence ledger;
- close issue #5 before the public tag, release, attachments, and public hash verification exist.
