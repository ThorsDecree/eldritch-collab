# VESTIGIA Runtime v0.7.0 validation record

Status: **validated code release; publication custody pending**

This record keeps deterministic CI, fresh-home onboarding, historical upgrade compatibility, live Discord evidence, and release custody separate. A passing deterministic test is not reported as a live Discord canary, and a fresh current-version home is not reported as an upgrade from the previous stable release.

No secret values, participant identifiers, channel identifiers, resident prose, message content, private image prompts, or private image bytes belong in this record.

## 1. Deterministic CI

The Windows matrix ran on Python 3.11 and Python 3.14.

It verified:

- source compilation;
- the complete deterministic test suite;
- package construction on Python 3.11;
- clean isolated installation of the built wheel with the `discord` and `tokens` extras;
- `pip check` after both development and isolated-wheel installation;
- imported stable version `0.7.0`.

Release-candidate Runtime CI run 34 (`30857072690`) passed on release head:

```text
c0d5138bd66bf5eadc9bdc5bc7ffdb094ce6a8d2
```

The package-smoke job hashed and uploaded:

```text
vestigia_runtime-0.7.0-py3-none-any.whl
vestigia_runtime-0.7.0.tar.gz
SHA256SUMS.txt
```

Preserved release-candidate artifact:

```text
vestigia-runtime-0.7.0-10497a2f7208d98364ebc13eba8a16b0c527cf63
```

Re-verified artifact ZIP SHA-256:

```text
ac9ab04d5fe1e49f31423e389599c0d1649ad27bd2c58dab17adf82af3282e09
```

Contained payload hashes:

```text
8cfe00302ce886c419760910994a61851dd3d0083404e38d724cc905fed5769b  vestigia_runtime-0.7.0-py3-none-any.whl
941cba3356b90948f35a8b86986f0561e7ed74d05ced04410faab23213004f13  vestigia_runtime-0.7.0.tar.gz
```

The post-merge `main` artifact remains the preferred release attachment under the release-custody rule recorded in PR #13.

## 2. Fresh-home canary

Date: 2026-08-03

A disposable resident home was onboarded from a preserved synthetic source transcript.

| Check | Result |
|---|---|
| Imported transcript turns | 6 |
| Explicitly inherited/unreviewed candidates | 4 |
| Explicit activation | Passed |
| Fake-provider resident turn | Passed |
| Context receipt | Created |
| Pack creation | Passed |
| Pack manifest hash verification during restore | Passed |
| Original state after turn | `ACTIVE` |
| Restored state | `ACTIVE` |
| Original turn count after turn | 8 |
| Restored turn count | 8 |

This proves current-version onboarding and current-version pack/restore. It is not the historical upgrade specimen.

## 3. Existing-home upgrade canary

The CI upgrade job constructed the specimen with genuine v0.6.1 repository commit:

```text
b12f8f778c390f4ac8fc3abe1af1a44a73799c26
```

The specimen was synthetic and contained no resident archive. Under the v0.6.1 source it created:

- one accepted resident-stated identity memory;
- one deterministic fake-provider exchange;
- one future bell;
- one fake private image and its artifact records;
- one queued image job;
- one pinned synthetic action receipt;
- one bounded workspace witness;
- an explicitly `ACTIVE` runtime state.

The v0.7.0 release candidate then:

```text
opened the v0.6.1 home
→ applied startup migrations
→ compared stable IDs, hashes, authority, state, and counts
→ completed a current fake-provider turn
→ created a home pack
→ verified the ZIP and pack manifest
→ restored into a new directory
→ opened the restored home
→ compared the durable ledgers again
```

Observed result:

- Runtime: `0.6.1` → `0.7.0`
- schema: `4` → `4`
- state: `ACTIVE` preserved
- memory/image/job/bell/receipt records preserved
- turns: 2 → 4 after the current turn
- restored durable counts exactly matched the post-turn home
- ZIP CRC and every pack-manifest file hash passed
- pack SHA-256: `76bc2b62dc9d34adb04e40e06ec27888cb822c3ef15f5dea7ce2301aedccfb81`
- evidence JSON contained no synthetic memory/message text or secret-shaped values

Preserved evidence artifact:

```text
v061-to-v070-upgrade-canary-10497a2f7208d98364ebc13eba8a16b0c527cf63
sha256:de93007f5ba5566779c3917d49cceb6ffc56a2b163d4bd7ed529a4547ec3b5d4
```

## 4. Live Windows Discord trust-boundary canary

The live gate was completed against the Windows v0.7.0 deployment. The extracted deployment contents were operator-verified by checksum against release head:

```text
c0d5138bd66bf5eadc9bdc5bc7ffdb094ce6a8d2
```

The deployment was not itself a Git checkout, so this is content verification rather than `git rev-parse` provenance.

Invalid or contaminated setup attempts are excluded below rather than relabeled as passes.

### Ambient source visibility

| Check | Live result | Redacted receipt/event reference |
|---|---|---|
| `source.visibility=hidden` | **Pass.** No ambient envelope created; preceding ambient text absent. | setting `receipt_20260803_230420_9e796520cf`; context `turn_20260803_230515_b836dbae31` |
| `source.visibility=allowlisted_only` | **Pass.** Allowlisted-human marker retained; non-allowlisted-human marker excluded. | setting `receipt_20260803_232406_256cfdc081`; context `turn_20260803_233543_0a441e7cc5` |
| `source.visibility=mentions_only` | **Pass on isolated history.** Mention-bearing ambient marker retained; ordinary marker and padding excluded. | setting `receipt_20260803_231455_3f3649bf6b`; context `turn_20260803_232115_a148d8388a` |
| `source.visibility=all_channel` | **Pass.** Preceding ambient text visible with stable message ID, `allowlisted` trust class, explicitly data-only. | setting `receipt_20260803_231029_542e08ed0b`; context `turn_20260803_231059_189cc24df8` |
| Oversized ambient message | **Pass.** Opening marker retained, ending marker truncated, stable ID/trust/data-only envelope preserved. | context `turn_20260803_234657_589ba3c244` |

The first non-isolated `mentions_only` attempt was inconclusive because older mention-bearing channel history remained in the adapter window. It is retained as a contaminated trial and is not counted as pass or failure. The isolated retest above is authoritative.

### Reaction authorization and delivery

| Check | Live result | Redacted receipt/event reference |
|---|---|---|
| React to triggering message | **Pass.** Authorization and Discord delivery succeeded; operator observed the reaction. | auth `receipt_20260803_224856_a4bed02705`; delivery `receipt_20260803_224858_c86fcd3309` |
| React to visible ambient message | **Pass.** Stable ambient ID authorized; Discord delivery succeeded; operator observed the reaction. | auth `receipt_20260803_234706_2e3b5f103e`; delivery `receipt_20260803_234711_cf687e51fc` |
| React to non-visible message ID | **Pass rejection.** Rejected before outward action; ordinary text survived; no retry. | rejected auth `receipt_20260803_224347_0e8f05d115` |
| Reaction removal | **Pass.** Add/remove separately authorized and delivered; operator observed appearance then removal. | auth `receipt_20260803_225733_8f5b8b5232`, `receipt_20260803_225733_a51b544109`; delivery `receipt_20260803_225737_acf1ad63cd`, `receipt_20260803_225737_5464e34294` |
| Failed reaction delivery | **Pass.** Local authorization succeeded, Discord delivery independently failed after Add Reactions was denied, ordinary reply remained visible, no reaction appeared, no retry occurred. | auth `receipt_20260804_025606_46aa99715a`; failed delivery `receipt_20260804_025608_1eb93dcfaf` |

An earlier failed-delivery setup attempt was rejected locally because its target was not exposed in the authenticated turn. It did not reach Discord and is not counted as the platform-failure case.

### Private-image challenge binding

| Check | Live result | Redacted receipt/event reference |
|---|---|---|
| Private-image challenge creation | **Pass.** Private asset returned `resident_confirmation_required`, `outward_action=false`; no attachment appeared. | `image.share` receipt `receipt_20260804_150104_00a66bc28a`; challenge `ch_20260804_150100_8599b211c8` |
| Later resident confirmation opened by another participant | **Pass.** Alternate human participant opened the later authenticated turn; resident-authored confirmation prepared the handoff; private attachment was operator-observed in the original channel. | auth `receipt_20260804_152119_cb83aeb4e4`; challenge `ch_20260804_152020_822c45f064`; created turn `turn_20260804_152017_51eb8637d2`; consumed turn `turn_20260804_152103_666216b4ad` |
| Wrong interface or channel | **Pass for wrong Discord channel.** Explicit destination mismatch; no outward action or attachment; challenge remained usable in the original channel. | `receipt_20260804_151402_d4d730836b` |
| Challenge replay | **Pass.** Consumed challenge explicitly rejected on a fresh later turn; no second outward action, attachment, or retry. | `receipt_20260804_153001_da4d75bb0d` |
| Expired challenge | **Pass.** Explicit expiry rejection; no outward action, attachment, or retry. | `receipt_20260804_150942_ae1a15ceba` |

The consumed challenge was durably observed as `status=consumed`, with `consumed_at=2026-08-04T15:21:10.849646+00:00`, before its recorded expiry. No separate attachment-delivery receipt appeared in the queried post-authorization sequence, so successful image delivery is recorded strictly as operator-observed rather than machine-receipted.

### Live gate conclusion

All live rows named by this validation record were exercised with redacted evidence.

**Live Discord trust-boundary gate: PASS.**

## 5. Merge and release-tree checkpoint

Release PR #13 merged into `main` at:

```text
748e5d74392ad4f0a98c75b187f82b91606e9e39
```

The merge was guarded with expected head SHA:

```text
c0d5138bd66bf5eadc9bdc5bc7ffdb094ce6a8d2
```

The PR discussion records a direct comparison between green PR merge ref:

```text
10497a2f7208d98364ebc13eba8a16b0c527cf63
```

and the actual `main` merge commit with **no file differences**. They are distinct merge commits over the same release tree.

The workflow at the merged commit was configured for push to `main` and should have produced artifacts named:

```text
vestigia-runtime-0.7.0-748e5d74392ad4f0a98c75b187f82b91606e9e39
v061-to-v070-upgrade-canary-748e5d74392ad4f0a98c75b187f82b91606e9e39
```

The next custody gate is to locate the exact `main` workflow run, confirm it succeeded, download the first artifact above, and independently verify its artifact and payload hashes.

## 6. Release custody sequence

```text
locate and verify exact post-merge main artifact
→ record its run/artifact/hash identity
→ merge custody documentation
→ tag the validated v0.7.0 release commit as v0.7.0
→ create the GitHub Release
→ attach exact verified wheel, source distribution, and SHA256SUMS.txt
→ download public attachments and verify hashes
→ update README/ROADMAP publication wording
→ close issue #5
```

A wheel rebuilt from later v0.8 development code is not a v0.7.0 release artifact and must never be represented as one.
